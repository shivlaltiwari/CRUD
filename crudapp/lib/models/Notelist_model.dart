class NoteForlisting {
  String noteID;
  String noteTitle;
  DateTime createDateTime;
  DateTime lastDateTime;

  NoteForlisting(
      {this.noteID, this.noteTitle, this.createDateTime, this.lastDateTime});

  
}
